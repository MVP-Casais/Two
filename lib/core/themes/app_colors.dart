import 'package:flutter/material.dart';

class AppColors {
  // Cores principais
  static const Color primary = Color(0xFFFF0505);
  static const Color secondary = Color(0xFFFFC1C1);
  static const Color terciary = Color(0xFFFF9797);

  // Cores de fundo
  static const Color background = Color(0xF5F5F5F5);
  static const Color neutral = Color(0xFFFFFFFF);
  static const Color icons = Color(0xFF2D2D2D);
  static const Color indicatorBackground = Color(0xFF2D2D2D);

  // Cores de texto
  static const Color titlePrimary = Color(0xFF2D2D2D);
  static const Color titleSecondary = Color(0xFF4F0000);
  static const Color textSecondarydark = Color(0xFF5D5D5D);
  static const Color textSecondarylight = Color(0xFF888888);

  // Cores Input
  static const Color inputBackground = Color(0x60E0E0E0);
  static const Color inputBorder = Color(0xFF888888);
  static const Color placeholder = Color(0xFF888888);

  // Cores de erro e sucesso
  static const Color error = Color(0xFFFF0505);
  static const Color success = Color(0xFF4CAF50);
}
